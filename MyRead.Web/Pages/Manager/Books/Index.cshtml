@page
@model MyRead.Web.Pages.Books.Manager.IndexModel
@{
    ViewData["Title"] = "List";
}

<h1>Book List</h1>
<table class="table">
    @foreach (var book in Model.Books)
    {
        <tr>
            <th>
                @book.Title
            </th>
            <th>
                @book.Author.FirstName @book.Author.LastName
            </th>
            <th>
                <a class="btn btn-primary" asp-page="./Edit" asp-route-bookId="@book.BookID">Edit</a>
            </th>
            <th>
                <a class="btn btn-danger" asp-page="./Delete" asp-route-bookId="@book.BookID">Delete</a>
            </th>
        </tr>
    }
</table>
<a class="btn btn-primary" asp-page="./Create">Add book</a>

